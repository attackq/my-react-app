import h from'./Header.module.css'

const Header = () => {
    return (
        <header className={h.header}>
            <div className={h.logo}>
                intouch
            </div>
        </header>
    )
}

export default Header;